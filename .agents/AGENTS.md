# Workspace Rules for ea-lumina

- Automatically commit and push changes to GitHub (`laramunique-a/ea-lumina`) after implementing and verifying requested features or bug fixes.
- Use clear Conventional Commits format for commit messages (e.g., `feat: ...`, `fix: ...`, `refactor: ...`).
- Ensure code compiles / passes build checks before committing and pushing.
